<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numads20_pankajbadgujar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pb_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".PowerChangeReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <activity
            android:name=".MathMagic"
            android:label="@string/mathMagicBtnText"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LinkCollector"
            android:label="@string/link_collector_btn_txt"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelloWorld"
            android:label="@string/hello_world_btn_txt"
            android:theme="@style/AppTheme.NoActionBar" />

        <receiver android:name=".WatchTimeReceiver"
            android:enabled="true"
            android:exported="true"
            />
    </application>

</manifest>